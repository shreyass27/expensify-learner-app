import React from 'react';
import { Link } from 'react-router-dom';

const NotFoundPage = () => (
    <div>
        $404 zhala ray baba <Link to="/" >Home la za ray - "to" use karnar tu</Link>
    </div>
);

export default NotFoundPage;